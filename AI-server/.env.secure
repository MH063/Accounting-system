# 安全环境变量配置
# 注意：在生产环境中，这些值应该从安全的密钥管理服务中获取

# 应用环境
NODE_ENV=production

# JWT密钥配置（256位随机字符串）
JWT_SECRET=your-secure-jwt-secret-key-here-minimum-32-characters
JWT_FALLBACK_SECRET=your-secure-jwt-fallback-secret-key-here-minimum-32-characters
JWT_EXPIRES_IN=24h
JWT_ROTATION_INTERVAL=30

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=accounting_system
DB_USER=accounting_user
DB_PASSWORD=your-database-password-here
DB_SSL=true

# 会话密钥（256位随机字符串）
SESSION_SECRET=your-session-secret-key-here-minimum-32-characters

# 日志配置
LOG_LEVEL=info
LOG_DIR=./logs

# 文件上传配置
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# 安全配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
